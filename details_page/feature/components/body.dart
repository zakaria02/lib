import 'package:flutter/material.dart';

class BudgetDetailsPageBody extends StatelessWidget {
  const BudgetDetailsPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Details"),
    );
  }
}
